<div class='container'>
  <div class='row'>
    <div class='col-md-12'>
      <h2>Follower Users</h2>
      <% if @users.size.positive? %>
        <%= render 'users/index', users: @users %>
      <% else %>
        ユーザはいません
      <% end %>
    </div>
  </div>
</div>
